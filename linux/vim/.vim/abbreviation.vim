"abbr detail http://vim.wikia.com/wiki/Using_abbreviations
ab #i #include 
ab std  #include <stdio.h>
ino cmain #include <stdio.h><cr><cr>int main(int argc, const char** argv)<cr>{<cr>return 0;<cr>}<ESC>2ko
ino fori for (int i = 0; i < ; i++){<cr><cr>}
ino while while (){<CR>}<ESC>kf(a
iab date Date: <C-R>=strftime("%Y-%m-%d %a %I:%M %p")<CR><Esc>
ino ' ''<ESC>i
ino " ""<ESC>i
imap ( ()<ESC>i
imap [ []<ESC>i
imap { {<CR>}<ESC>O
ino if if (){<CR>}<ESC>kf(a
