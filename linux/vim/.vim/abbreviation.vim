"abbr detail http://vim.wikia.com/wiki/Using_abbreviations
iabbr #i #include 
ab std  #include <stdio.h>
ab cmain #include <stdio.h><cr><cr>int main(int argc, const char** argv)<cr>{<cr>return 0;<cr>}
iabbr fori for (int i = 0; i < 100; i++){<cr><cr>}
iab date Date: <C-R>=strftime("%Y-%m-%d %a %I:%M %p")<CR><Esc>
imap ( ()<ESC>i
imap { {<CR>}<ESC>O

